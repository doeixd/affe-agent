/** Provider-neutral web search capability and domain values. */
export * as WebSearch from "./WebSearch.js"

/** Provider-neutral guarded web fetch capability and domain values. */
export * as WebFetch from "./WebFetch.js"

/** Provider-neutral rendered-page capture: a page as Markdown, with its links. */
export * as WebCapture from "./WebCapture.js"

/** A bounded same-host crawl over `WebCapture`. Portable. */
export * as WebCrawl from "./WebCrawl.js"

/** Model-facing web tools and ordinary Effect AI handlers. */
export * as WebToolkit from "./WebToolkit.js"
