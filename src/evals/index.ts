/**
 * Behavioural evals: run an agent through its public interface and assert on
 * what it did -- tools called, turns taken, reply shape, an LLM judge. Separate
 * from `/testing`; see `Evals`.
 */
export * as Evals from "./Evals.js"
