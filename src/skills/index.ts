/**
 * Skills: on-demand capabilities the model loads only when it needs them.
 *
 * A registry service, one context transform that advertises metadata, and one
 * tool that loads a body -- the OpenCode loading strategy over the seams the
 * library already has. See `Skills`. It adds no capability to the engine.
 */
export * as Skills from "./Skills.js"
