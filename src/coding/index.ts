/**
 * A batteries-included coding toolkit over the sandbox seam: read, write,
 * edit, list, search and bash, each with a `Permission` projection.
 */
export * as CodingToolkit from "./CodingToolkit.js"
