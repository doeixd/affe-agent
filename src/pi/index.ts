/**
 * A second coding toolkit, with Pi's tool contracts.
 *
 * `/coding` is the OpenCode-shaped battery. This one is for agents authored
 * against Pi's schemas: batch `edits[]`, rendered `list_files`, an injectable
 * shell. Same sandbox, same permission projections, different contracts.
 * See `docs/plan-pi-toolkit.md`.
 */
export * as PiToolkit from "./PiToolkit.js"
