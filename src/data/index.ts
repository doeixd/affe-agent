/**
 * Structured client/UI data: Schema-first named channels a tool writes to and a
 * UI or transport reads, typed on both ends. Observational -- it never touches
 * canonical history. See `AgentData`.
 */
export * as AgentData from "./AgentData.js"
